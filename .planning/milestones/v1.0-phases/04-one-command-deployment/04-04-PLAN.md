---
phase: 04-one-command-deployment
plan: 04
type: execute
wave: 3
depends_on: ["04-03"]
files_modified:
  - test/tests/02-watchlist.spec.ts
  - test/tests/03-trading.spec.ts
  - test/tests/04-visualizations.spec.ts
  - test/tests/05-chat.spec.ts
  - test/tests/06-sse-reconnect.spec.ts
autonomous: true
requirements: [TEST-05]

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A ticker added through the watchlist input appears as a new grid row, and removing it through its Remove control makes that row disappear"
    - "A buy through the trade bar decreases the cash balance and creates a position row in the positions table; a subsequent sell increases the cash balance and reduces or clears that position"
    - "With at least one open position, the portfolio heatmap renders a cell for that position and the P&L chart panel renders its chart instead of its empty state"
    - "Sending a trade instruction in the AI chat panel with LLM_MOCK=true returns an assistant message and renders the trade confirmation inline in the conversation"
    - "Dropping the browser's network connection moves the header connection indicator out of the open state, and restoring it returns the indicator to open with prices resuming"
    - "The whole compose E2E suite runs green end to end, covering all six TEST-05 scenarios in a single command"
  prohibitions:
    - statement: "MUST NOT report the E2E suite as passing when a named TEST-05 scenario is not actually exercised — no skipped tests, no assertion-free spec bodies, no always-true filler assertions"
      category: transparency
      status: resolved
      verification: judgment
      flagged: true
  artifacts:
    - path: "test/tests/02-watchlist.spec.ts"
      provides: "Watchlist add and remove E2E scenario"
      min_lines: 20
    - path: "test/tests/03-trading.spec.ts"
      provides: "Buy and sell E2E scenario with delta-based cash and position assertions"
      min_lines: 25
    - path: "test/tests/04-visualizations.spec.ts"
      provides: "Portfolio heatmap and P&L chart rendering E2E scenario"
      min_lines: 20
    - path: "test/tests/05-chat.spec.ts"
      provides: "AI chat E2E scenario with an inline trade confirmation under LLM_MOCK"
      min_lines: 20
    - path: "test/tests/06-sse-reconnect.spec.ts"
      provides: "SSE disconnect and reconnect E2E scenario"
      min_lines: 20
  key_links:
    - from: "test/tests/03-trading.spec.ts"
      to: "frontend/components/TradeBar.tsx"
      via: "drives the trade-ticker-input and trade-quantity-input fields and the Buy/Sell buttons"
      pattern: "trade-ticker-input"
    - from: "test/tests/05-chat.spec.ts"
      to: "backend/app/llm/mock.py"
      via: "relies on the deterministic LLM_MOCK trade-sentence matcher to produce an inline trade action"
      pattern: "chat-send"
    - from: "test/tests/06-sse-reconnect.spec.ts"
      to: "frontend/hooks/usePriceStream.ts"
      via: "asserts the ConnectionState transitions surfaced as the header's Connection text"
      pattern: "Connection:"
---

<objective>
Complete the TEST-05 suite: watchlist add/remove, buy and sell, heatmap and P&L chart rendering, AI chat with an inline trade, and SSE reconnection — all against the containerized app started by `test/docker-compose.test.yml`.

Purpose: this is the "verified" half of the phase goal. Without these five scenarios the milestone can only claim the app builds and starts, not that it works. Each scenario maps directly to a ROADMAP Phase 4 success-criterion clause.

Output: `test/tests/02-watchlist.spec.ts`, `03-trading.spec.ts`, `04-visualizations.spec.ts`, `05-chat.spec.ts`, `06-sse-reconnect.spec.ts`.
</objective>

## Phase Goal

**Goal (verbatim from ROADMAP.md Phase 4):** Anyone can launch the whole verified app with a single command and keep their portfolio across restarts

> MVP_MODE is enabled but this ROADMAP `**Goal:**` line is not in the canonical "As a … I want to … so that …" user-story form. No story is invented here; run `/gsd mvp-phase 4` if user-story framing is wanted in ROADMAP.md.

## Artifacts this phase produces

Symbols created by Phase 04 (none exist before this phase — exclude from drift/source-grounding verification):

**New files (04-01):** `Dockerfile`, `.dockerignore`, `db/.gitkeep`, `scripts/verify_container.sh`
**New files (04-02):** `scripts/start_mac.sh`, `scripts/stop_mac.sh`, `scripts/start_windows.ps1`, `scripts/stop_windows.ps1`
**New files (04-03):** `test/package.json`, `test/package-lock.json`, `test/playwright.config.ts`, `test/docker-compose.test.yml`, `test/.gitignore`, `test/tests/01-fresh-start.spec.ts`
**New files (this plan):** `test/tests/02-watchlist.spec.ts`, `test/tests/03-trading.spec.ts`, `test/tests/04-visualizations.spec.ts`, `test/tests/05-chat.spec.ts`, `test/tests/06-sse-reconnect.spec.ts`
**New env vars:** `FINALLY_DB_PATH`, `UV_PYTHON_DOWNLOADS=0`, `BASE_URL`
**New CLI flags introduced by this phase:** `uvicorn --timeout-graceful-shutdown 10`, `docker run --stop-timeout 15`, `docker stop --timeout 15`, `--build` (lifecycle scripts)
**New image/container names:** image `finally`, containers `finally-app` and `finally-verify`; compose services `app` and `playwright`
**New npm package introduced by this phase:** `@playwright/test` (audited and human-approved in 04-03)
**Deliberately NOT produced:** a root-level `docker-compose.yml`

<execution_context>
@/home/tamer/AICouseProjects/finally/.claude/gsd-core/workflows/execute-plan.md
@/home/tamer/AICouseProjects/finally/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-one-command-deployment/04-CONTEXT.md
@.planning/phases/04-one-command-deployment/04-RESEARCH.md
@.planning/phases/04-one-command-deployment/04-03-SUMMARY.md
@frontend/app/page.tsx
@backend/app/llm/mock.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Watchlist and trading E2E scenarios</name>
  <precondition>`test/playwright.config.ts` and `test/docker-compose.test.yml` exist and the compose suite already runs green with `01-fresh-start.spec.ts` (delivered by plan 04-03)</precondition>
  <files>test/tests/02-watchlist.spec.ts, test/tests/03-trading.spec.ts</files>
  <read_first>
    - `frontend/components/WatchlistPanel.tsx` — `data-testid="watchlist-add-input"` (line 169) and the add form's submit button (lines 164-177), `data-testid="watchlist-grid"` (line 181), `data-testid={`watchlist-row-${ticker}`}` (line 204), and the per-row remove button carrying `aria-label={`Remove ${ticker}`}` (lines 231-241)
    - `frontend/components/TradeBar.tsx` — `data-testid="trade-ticker-input"` (line 73), `data-testid="trade-quantity-input"` (line 83), and the two submit buttons whose accessible names are `Buy` and `Sell` (lines 86-101); note both show `Submitting…` while in flight
    - `frontend/components/PositionsTable.tsx` — `data-testid="positions-grid"` (line 90) and the column set the assertions read
    - `frontend/app/page.tsx` — the header's `Cash {formatCurrency(...)}` text (line 65) used for the cash-delta assertions
    - `backend/app/portfolio/router.py` and `backend/app/portfolio/trades.py` — the trade validation rules (buys need sufficient cash, sells need sufficient held quantity, never clamped) so the specs choose quantities that always validate
    - `test/tests/01-fresh-start.spec.ts` — the assertion style and locator conventions established in 04-03
  </read_first>
  <action>
Create two spec files. The numeric prefixes are part of the contract: the whole suite runs serially in a single worker against one shared seeded portfolio, so `02-watchlist` runs after the fresh-start baseline and before `03-trading` mutates cash. Neither spec may assert an absolute cash figure — only `01-fresh-start.spec.ts` gets to do that, because only it is guaranteed to see the pristine seeded starting balance. Everything from here asserts deltas and presence.

`test/tests/02-watchlist.spec.ts` covers the ROADMAP's "watchlist add/remove" clause. Navigate to `/`. Pick a ticker that is not among the ten seeded ones so the test cannot collide with seed data — use `PYPL`. Type it into the `watchlist-add-input` field, submit the add form, and assert a `watchlist-row-PYPL` row becomes visible and that the grid's row count increased by exactly one relative to a count captured before the add. Then click that row's remove control, located by its accessible name `Remove PYPL`, and assert the `watchlist-row-PYPL` row is detached and the row count returns to its pre-add value. Leave the watchlist in the state it started in so later specs see the ten seeded tickers.

`test/tests/03-trading.spec.ts` covers the ROADMAP's "buy and sell" clause. Navigate to `/`. Capture the header's cash text before trading. Fill `trade-ticker-input` with `AAPL` and `trade-quantity-input` with `2`, click the button whose accessible name is `Buy`, and then assert three things: the header cash value has strictly decreased from the captured baseline, an `AAPL` row is visible in `positions-grid`, and that row shows a quantity of 2. Parse the currency text into a number before comparing — the header renders thousands separators and a leading dollar sign, so a string comparison would be wrong. Then sell part of the position: fill the same fields with `AAPL` and `1`, click the button whose accessible name is `Sell`, and assert the cash value has strictly increased from its post-buy value and the `AAPL` position row now shows a quantity of 1. Leave that 1 share open — `04-visualizations.spec.ts` depends on at least one open position existing, which is exactly why the trading spec is numbered before it.

Prices tick roughly every 500ms, so the cash figure moves independently of trades through unrealized-P&L-driven total-value updates. Assert on the direction of the cash change relative to the captured baseline rather than on an exact arithmetic result, and read the baseline immediately before each action to keep the window tight. Use `expect.poll` with the config's expect timeout for values that settle asynchronously after the trade round-trip.

Every test body must carry at least one real assertion, and no test may be focused or carry a test-level skip modifier — a green run that never exercised its named scenario would misreport this phase as verified.
  </action>
  <verify>
    <automated>docker compose -f test/docker-compose.test.yml up --build --abort-on-container-exit --exit-code-from playwright; code=$?; docker compose -f test/docker-compose.test.yml down -v; exit $code</automated>
  </verify>
  <acceptance_criteria>
    - `docker compose -f test/docker-compose.test.yml up --build --abort-on-container-exit --exit-code-from playwright` exits 0
    - The reporter output lists passing tests from `01-fresh-start.spec.ts`, `02-watchlist.spec.ts`, and `03-trading.spec.ts`, in that file order, with zero skipped
    - `grep -q 'PYPL' test/tests/02-watchlist.spec.ts` succeeds and `grep -q 'Remove PYPL' test/tests/02-watchlist.spec.ts` succeeds
    - `grep -q 'trade-ticker-input' test/tests/03-trading.spec.ts` and `grep -q 'positions-grid' test/tests/03-trading.spec.ts` both succeed
    - `grep -c 'expect(' test/tests/02-watchlist.spec.ts` returns at least 3 and the same for `test/tests/03-trading.spec.ts`
    - `grep -v '^\s*//' test/tests/02-watchlist.spec.ts | grep -cE 'test\.only|\.skip\(' ` returns 0, and the same for `test/tests/03-trading.spec.ts`
    - Neither new spec contains the literal `10,000.00` — absolute cash assertions belong only to the fresh-start spec
    - `md5sum db/finally.db` is unchanged across the compose run
  </acceptance_criteria>
  <done>The compose suite runs green with the watchlist add/remove and buy/sell scenarios passing against the container, leaving one open AAPL share for the visualization spec.</done>
</task>

<task type="auto">
  <name>Task 2: Visualization, AI chat, and SSE reconnection E2E scenarios</name>
  <files>test/tests/04-visualizations.spec.ts, test/tests/05-chat.spec.ts, test/tests/06-sse-reconnect.spec.ts</files>
  <read_first>
    - `frontend/components/PortfolioHeatmap.tsx` — each position cell is a `role="button"` element carrying `aria-label={`${ticker}: ${sign}${pct}% unrealized P&L`}` (lines 68-69); that accessible name is the stable locator, there is no test id
    - `frontend/components/PnlChart.tsx` — the panel heading span (lines 98-101) and the `Building portfolio history` empty-state overlay (lines 106-110) that must be gone once snapshots exist; the chart itself is a lightweight-charts canvas inside the container div at line 105
    - `frontend/components/ChatDrawer.tsx` — `data-testid="chat-toggle"` (lines 59, 79), `data-testid="chat-drawer"` (line 69), `data-testid="chat-input"` (line 116), `data-testid="chat-send"` (line 123)
    - `frontend/components/ChatMessageList.tsx` and `frontend/components/ChatMessageBubble.tsx` — `data-testid="chat-message-list"` (line 38), `data-testid="chat-thinking"` (line 48), and `data-testid="chat-message-assistant"` / `chat-message-user` (line 37)
    - `frontend/components/TradeConfirmationCard.tsx` — `data-testid="trade-card"` (line 41) for an executed trade and `data-testid="trade-card-rejected"` (line 25) for a rejected one
    - `backend/app/llm/mock.py` — the deterministic matcher: `_TRADE_RE` matches `buy|sell <qty> [shares of] <ticker>`, and `_trade_sentence` renders `"{Side}ing {qty:g} {TICKER}."`, so the message for a buy of 2 AAPL is exactly `Buying 2 AAPL.`
    - `frontend/hooks/usePriceStream.ts` — `ConnectionState` is `"connecting" | "open" | "reconnecting" | "closed"` (line 15) and the error handler picks `closed` vs `reconnecting` from `EventSource.readyState` (lines 47-48)
    - `frontend/app/page.tsx` — the header renders `Connection: {status}` (line 71) beside a colored dot; that text is the reconnection assertion anchor
  </read_first>
  <action>
Create three spec files, numbered to preserve the serial order established by the earlier specs.

`test/tests/04-visualizations.spec.ts` covers the ROADMAP's "heatmap and P&L chart rendering" clause and depends on the open AAPL position left behind by `03-trading.spec.ts`. Navigate to `/`. Assert the portfolio heatmap renders a cell for the held ticker by locating the element with role `button` whose accessible name starts with `AAPL:` and asserting it is visible; also assert its accessible name matches a percentage-bearing pattern so a rendered-but-empty cell fails. Assert the P&L panel has resolved out of its empty state: the `Building portfolio history` text must not be visible, and the chart container must have a non-zero bounding box with a canvas element inside it. Snapshots are recorded every 30 seconds and immediately after each trade, so `03-trading.spec.ts`'s two trades guarantee data exists by the time this spec runs — do not add a fixed sleep, use `expect` with the config timeout.

`test/tests/05-chat.spec.ts` covers the ROADMAP's "AI chat with an inline trade" clause and relies on `LLM_MOCK=true`, which the compose app service sets. Navigate to `/`, open the chat panel via `chat-toggle` if it is not already open, type `buy 2 shares of AAPL` into `chat-input`, and click `chat-send`. Assert three things: a `chat-message-user` bubble appears carrying the sent text; a `chat-message-assistant` bubble appears carrying exactly `Buying 2 AAPL.` — that string is the mock matcher's deterministic output for this input, which is precisely why the mock exists; and a `trade-card` element renders inline in the conversation showing the executed AAPL buy. Assert the `chat-thinking` indicator is gone once the response lands. This spec deliberately runs after the trading spec so there is ample cash for the mock-driven buy to validate rather than be rejected.

`test/tests/06-sse-reconnect.spec.ts` covers the ROADMAP's "SSE reconnection after a disconnect" clause. Navigate to `/` and first assert the header text reaches `Connection: open`. Then take the browser context offline with `context.setOffline(true)` and assert the header text leaves the open state — accept either `reconnecting` or `closed`, since which one appears depends on whether the browser reports the EventSource as terminally closed, and pinning to one of them would make the test brittle for no gain. Then restore connectivity with `context.setOffline(false)` and assert the header returns to `Connection: open` and that a watchlist price value updates again afterwards, proving the stream genuinely resumed rather than the indicator merely flipping. Give the reconnect assertion a generous bounded wait — `EventSource`'s native retry backoff is not instantaneous — but never an unbounded one and never a fixed sleep as the assertion itself.

Every test body must carry at least one real assertion; no focused tests, and no test-level skip modifiers anywhere in the suite.

Once all six specs exist, the task's gate is the full compose run — this is the moment TEST-05 is either satisfied or not.
  </action>
  <verify>
    <automated>docker compose -f test/docker-compose.test.yml up --build --abort-on-container-exit --exit-code-from playwright; code=$?; docker compose -f test/docker-compose.test.yml down -v; exit $code</automated>
  </verify>
  <acceptance_criteria>
    - `docker compose -f test/docker-compose.test.yml up --build --abort-on-container-exit --exit-code-from playwright` exits 0
    - The reporter output lists passing tests from all six spec files — `01-fresh-start`, `02-watchlist`, `03-trading`, `04-visualizations`, `05-chat`, `06-sse-reconnect` — with zero skipped and zero failed
    - Running that compose command a second time also exits 0, confirming the ephemeral database makes the suite repeatable
    - `grep -q 'Buying 2 AAPL.' test/tests/05-chat.spec.ts` succeeds (the exact deterministic mock output is asserted, not a loose substring)
    - `grep -q 'trade-card' test/tests/05-chat.spec.ts` succeeds
    - `grep -q 'setOffline' test/tests/06-sse-reconnect.spec.ts` succeeds and `grep -q 'Connection:' test/tests/06-sse-reconnect.spec.ts` succeeds
    - `grep -q 'Building portfolio history' test/tests/04-visualizations.spec.ts` succeeds (the empty state is explicitly asserted absent)
    - `grep -c 'expect(' test/tests/04-visualizations.spec.ts`, `grep -c 'expect(' test/tests/05-chat.spec.ts`, and `grep -c 'expect(' test/tests/06-sse-reconnect.spec.ts` each return at least 3
    - `grep -v '^\s*//' test/tests/0*.spec.ts | grep -cE 'test\.only|\.skip\(' ` returns 0 across the whole suite
    - `ls test/tests/*.spec.ts | wc -l` returns 6
    - `md5sum db/finally.db` is unchanged across the compose run
  </acceptance_criteria>
  <done>All six TEST-05 scenarios pass in a single `docker compose -f test/docker-compose.test.yml up ...` command against the production image, twice in a row, without touching the developer's database.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test app container `/app/db` → host filesystem | Must remain a dead end; the suite mutates a portfolio heavily and must never reach real user data |
| test container → external network | The suite must not call OpenRouter or Massive; a network-dependent E2E run is both non-deterministic and a cost/credential exposure |
| Playwright suite result → phase verification verdict | A vacuous green run would falsely certify the milestone as complete |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-05 | Tampering | E2E trades versus the developer's `db/finally.db` | high | mitigate | Inherited from 04-03: `tmpfs: /app/db` on the compose app service, no host mount; re-verified here by comparing the host database checksum across a suite run that executes real buys and sells |
| T-04-09 | Spoofing (false verification signal) | Playwright suite integrity | medium | mitigate | `forbidOnly: true` in the config, plus filtered negative greps for focused and skipped tests across all six specs, plus a minimum assertion count per spec |
| T-04-08 | Information Disclosure | outbound LLM calls during the suite | medium | mitigate | `LLM_MOCK: "true"` and `MASSIVE_API_KEY: ""` on the compose app service mean the chat spec exercises `backend/app/llm/mock.py` and never transmits a prompt or a key off the machine |
| T-04-04 | Elevation of Privilege | test containers running as root | low | accept | Ephemeral, localhost-only containers with no durable state (04-RESEARCH.md Open Question 2) |
</threat_model>

<flagged_assumptions>
Spec-less probe fallback (no SPEC.md exists for Phase 04). The two classified TEST-05 probe rows are resolved and authored into `must_haves.truths` in plan `04-03` (idempotency: fresh ephemeral database every run; concurrency: `workers: 1` + `fullyParallel: false`). This plan is the one that makes both facts load-bearing, because it is where multiple state-mutating specs first coexist — the serial ordering contract encoded in the `NN-` filename prefixes is the concrete mechanism.

The three `unclassified`/unresolved probe rows (DEPLOY-01, DEPLOY-02, DEPLOY-03) are surfaced in plans `04-01` and `04-02`; none of them apply to this plan.
</flagged_assumptions>

<verification>
- `docker compose -f test/docker-compose.test.yml up --build --abort-on-container-exit --exit-code-from playwright` exits 0, twice in a row
- Six spec files exist under `test/tests/` and all report passing tests with zero skipped
- The host `db/finally.db` checksum is unchanged across a suite run that performs real trades
</verification>

<success_criteria>
- All six ROADMAP Phase 4 E2E scenarios pass in one command against the production container image
- The suite is deterministic and offline: mocked LLM, simulator market data, ephemeral database
- No scenario greens without actually being exercised
</success_criteria>

<output>
Create `.planning/phases/04-one-command-deployment/04-04-SUMMARY.md` when done
</output>
